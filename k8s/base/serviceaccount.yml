apiVersion: v1
kind: ServiceAccount
metadata:
  name: ledger-api
  namespace: payments
# ledger-api never calls the Kubernetes API, so no token is mounted at all —
# stronger than scoping a Role down; there's simply nothing to steal.
automountServiceAccountToken: false